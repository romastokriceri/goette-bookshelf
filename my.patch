diff --git a/pages/index.js b/pages/index.js
index dfd794ea15d1f8566d075055415e319436e11643..3c5aa2d31d6952c10cba502dfa9a8ff41dc12b84 100644
--- a/pages/index.js
+++ b/pages/index.js
@@ -1,28 +1,29 @@
 import React, { useState, useEffect } from 'react';
 import {
   X,
+  Menu,
   BookOpen,
   User,
   MessageCircle
 } from 'lucide-react';
 import { 
   authorData, 
   shelves, 
   heroData, 
   translations,
   shelfDividerQuotes,
   bookQuotes,
   articlesData,
   reviewsData
 } from '../data';
 
 /* =======================
    Utils
 ======================= */
 const getR2Url = (path) => {
   const R2_URL = process.env.NEXT_PUBLIC_R2_URL || '';
   return `${R2_URL}/${path}`;
 };
 
 /* =======================
    ReviewCard Component
@@ -93,131 +94,154 @@ const Comments = () => {
     (document.head || document.body).appendChild(script);
 
     return () => {
       if (window.DISQUS) window.DISQUS.reset({ reload: true });
     };
   }, []);
 
   return (
     <div>
       <div id="disqus_thread"></div>
       <noscript>Please enable JavaScript to view the comments.</noscript>
     </div>
   );
 };
 
 /* =======================
    Main Component (Home)
 ======================= */
 export default function Home() {
   const [selectedBook, setSelectedBook] = useState(null);
   const [selectedArticle, setSelectedArticle] = useState(null);
   const [expandedReview, setExpandedReview] = useState(null);
   const [lang, setLang] = useState('ru');
   const [expandedQuotes, setExpandedQuotes] = useState({});
   const [isMobile, setIsMobile] = useState(false);
+  const [isMenuOpen, setIsMenuOpen] = useState(false);
 
   useEffect(() => {
     const savedLang = localStorage.getItem('selectedLanguage') || 'ru';
     setLang(savedLang);
     
     const checkMobile = () => {
       setIsMobile(/iPhone|iPad|iPod|Android/i.test(navigator.userAgent));
     };
     checkMobile();
     
     window.addEventListener('resize', checkMobile);
     return () => window.removeEventListener('resize', checkMobile);
   }, []);
 
+  useEffect(() => {
+    const closeMenuOnDesktop = () => {
+      if (window.innerWidth > 768) {
+        setIsMenuOpen(false);
+      }
+    };
+
+    window.addEventListener('resize', closeMenuOnDesktop);
+    return () => window.removeEventListener('resize', closeMenuOnDesktop);
+  }, []);
+
   const switchLang = (newLang) => {
     setLang(newLang);
     localStorage.setItem('selectedLanguage', newLang);
+    setIsMenuOpen(false);
   };
   
   const toggleQuote = (id) => {
     setExpandedQuotes(prev => ({ ...prev, [id]: !prev[id] }));
   };
 
   const scrollToSection = (id) => {
     const element = document.getElementById(id);
     if (!element) return;
     const headerOffset = 80;
     const elementPosition = element.getBoundingClientRect().top;
     const offsetPosition = elementPosition + window.pageYOffset - headerOffset;
     window.scrollTo({ top: offsetPosition, behavior: 'smooth' });
+    setIsMenuOpen(false);
   };
 
   const quoteById = Object.values(shelfDividerQuotes).flat().reduce((acc, quote) => {
     acc[quote.id] = quote;
     return acc;
   }, {});
 
   const renderQuote = (quote) => {
     const isExpanded = expandedQuotes[quote.id];
     const text = quote.text[lang];
     const isLongText = text.length > 200;
 
     return (
       <div key={quote.id} className="shelf-divider-quote">
         <div className="divider-quote-content">
           <div className={`quote-wrapper ${isExpanded || !isLongText ? 'expanded' : ''}`}>
             <blockquote className="divider-quote-text">
               <p>{text}</p>
             </blockquote>
             {isLongText && !isExpanded && <div className="quote-gradient-overlay"></div>}
           </div>
 
           {isLongText && (
             <button
               className="quote-toggle-btn"
               onClick={() => toggleQuote(quote.id)}
             >
               {isExpanded ? translations[lang].buttons.readLess : translations[lang].buttons.readMore}
             </button>
           )}
 
           <footer className="divider-quote-footer">
             <strong>{quote.author[lang]}</strong>
             <br />
             <span className="divider-quote-position">
               {quote.position[lang]}
             </span>
           </footer>
         </div>
       </div>
     );
   };
 
   return (
     <div className="app-wrapper">
       {/* HEADER */}
-      <header className="sticky-header">
+      <header className={`sticky-header ${isMenuOpen ? 'menu-open' : ''}`}>
         <nav className="nav-container">
           <div className="logo">{authorData.name[lang]}</div>
+
+          <button
+            className="menu-toggle"
+            aria-label="Toggle navigation"
+            aria-expanded={isMenuOpen}
+            onClick={() => setIsMenuOpen((prev) => !prev)}
+          >
+            {isMenuOpen ? <X size={24} /> : <Menu size={24} />}
+          </button>
           
-          <ul className="nav-links">
+          <ul className={`nav-links ${isMenuOpen ? 'open' : ''}`}>
             {['home', 'about', 'books', 'articles', 'reviews', 'feedback'].map((sec) => (
               <li key={sec} onClick={() => scrollToSection(sec)}>
                 {translations[lang].nav[sec]}
               </li>
             ))}
           </ul>
 
           <div className="lang-switcher">
             {['ru', 'de', 'en'].map((l) => (
               <button 
                 key={l}
                 className={lang === l ? 'active' : ''}
                 onClick={() => switchLang(l)}
               >
                 {l.toUpperCase()}
               </button>
             ))}
           </div>
         </nav>
       </header>
 
       <main className="container">
         {/* HERO SECTION */}
         <section id="home" className="hero-section">
           <div className="hero-card">
@@ -442,26 +466,26 @@ export default function Home() {
       {selectedArticle && !isMobile && (
         <div className="pdf-modal" onClick={() => setSelectedArticle(null)}>
           <div className="modal-content" onClick={(e) => e.stopPropagation()}>
             <div className="modal-header">
               <div className="modal-info">
                 <h3 className="modal-title">{selectedArticle.title[lang]}</h3>
               </div>
               <button className="close-btn" onClick={() => setSelectedArticle(null)}>
                 <X size={32} />
               </button>
             </div>
             <div className="iframe-container">
               <iframe
                 src={`${getR2Url(`article/${encodeURIComponent(selectedArticle.pdfFile)}`)}#toolbar=1`}
                 title="Article PDF Viewer"
                 width="100%"
                 height="100%"
               />
             </div>
           </div>
         </div>
       )}
 
     </div>
   );
-}
\ No newline at end of file
+}
diff --git a/public/fonts/README.md b/public/fonts/README.md
new file mode 100644
index 0000000000000000000000000000000000000000..cf60a42d6cc1360b6bd3966c81d646f6cda18a7d
--- /dev/null
+++ b/public/fonts/README.md
@@ -0,0 +1,5 @@
+Place TT Lovelies Script font files here for web loading:
+- TT-Lovelies-Script.woff2
+- TT-Lovelies-Script.woff
+
+The site already references these files via @font-face in styles/globals.css.
diff --git a/styles/globals.css b/styles/globals.css
index 004acfabd6b998a6dbd2837ff4861a73faa5c5ce..a848635522f2cd5ea1173800dc6ad5847cbc8a53 100644
--- a/styles/globals.css
+++ b/styles/globals.css
@@ -1,108 +1,133 @@
+@font-face {
+  font-family: 'TT Lovelies Script';
+  src:
+    local('TT Lovelies Script'),
+    local('TTLoveliesScript'),
+    url('/fonts/TT-Lovelies-Script.woff2') format('woff2'),
+    url('/fonts/TT-Lovelies-Script.woff') format('woff');
+  font-display: swap;
+}
+
 :root {
   --wood-color: #8b5a2b;
   --wood-dark: #5d3a1a;
   --bg-color: #f9f7f2;
   --text-main: #2c3e50;
   --accent: #d35400;
   --shelf-shadow: rgba(0, 0, 0, 0.3);
+  --body-font-scale: 1.05;
+  --body-line-scale: 0.9;
 }
 
 /* =====================
    RESET & BASE
    ===================== */
 
 * {
   box-sizing: border-box;
   margin: 0;
   padding: 0;
 }
 
 body {
   font-family: 'Georgia', serif;
   background-color: var(--bg-color);
   color: var(--text-main);
-  line-height: 1.6;
-  font-size: 110%;
+  line-height: calc(1.6 * var(--body-line-scale));
+  font-size: calc(110% * var(--body-font-scale));
 }
 
 /* =====================
    HEADER / NAV
    ===================== */
 
 .sticky-header {
   position: fixed;
   top: 0;
   width: 100%;
   background: rgba(255, 255, 255, 0.95);
   backdrop-filter: blur(5px);
   border-bottom: 3px solid var(--wood-color);
   z-index: 1000;
-  padding: 1rem 0;
+  padding: 0.85rem 0;
 }
 
 .nav-container {
-  max-width: 1100px;
+  max-width: 1280px;
   margin: 0 auto;
   padding: 0 20px;
   display: flex;
   justify-content: space-between;
   align-items: center;
-  gap: 20px;
+  gap: 14px;
 }
 
 .logo {
   font-size: 1.4rem;
   font-weight: bold;
   color: var(--wood-dark);
   text-transform: uppercase;
   white-space: nowrap;
 }
 
 .nav-links {
   display: flex;
-  gap: 25px;
+  gap: clamp(12px, 1.6vw, 22px);
   list-style: none;
   flex-wrap: wrap;
+  justify-content: center;
+  flex: 1;
 }
 
 .nav-links li {
   cursor: pointer;
   font-weight: 600;
   transition: color 0.3s;
   white-space: nowrap;
 }
 
 .nav-links li:hover {
   color: var(--accent);
 }
 
 /* Language Switcher */
 .lang-switcher {
   display: flex;
   gap: 8px;
   align-items: center;
+  flex-shrink: 0;
+}
+
+.menu-toggle {
+  display: none;
+  border: 2px solid var(--wood-color);
+  border-radius: 6px;
+  background: #fff;
+  color: var(--wood-dark);
+  padding: 6px;
+  cursor: pointer;
 }
 
 .lang-switcher button {
   padding: 6px 14px;
   font-size: 0.9rem;
   font-weight: 600;
   border: 2px solid var(--wood-color);
   background: transparent;
   color: var(--wood-dark);
   border-radius: 4px;
   cursor: pointer;
   transition: all 0.3s ease;
   font-family: 'Georgia', serif;
 }
 
 .lang-switcher button:hover {
   background: rgba(139, 90, 43, 0.1);
 }
 
 .lang-switcher button.active {
   background: var(--wood-color);
   color: white;
 }
 
 /* =====================
@@ -191,79 +216,79 @@ body {
 .hero-quote-top-right {
   margin: 0;
 }
 
 .hero-divider {
   width: 120px;
   height: 3px;
   background: linear-gradient(90deg, transparent, var(--wood-color), transparent);
   margin: 0 auto;
 }
 
 .hero-quote {
   background: rgba(139, 90, 43, 0.06);
   border-left: 4px solid var(--accent);
   padding: 20px 24px;
   margin: 20px 0;
   border-radius: 4px;
   font-style: italic;
 }
 
 .quote-text {
   font-size: 1.55rem;
   color: #1e2f44;
   font-weight: 500;
   margin-bottom: 10px;
-  line-height: 1.45;
-  font-family: "Marck Script", "Caveat", "Segoe Script", cursive;
+  line-height: 1.62;
+  font-family: "TT Lovelies Script", "Marck Script", "Caveat", "Segoe Script", cursive;
 }
 
 .quote-author {
   font-style: normal;
   font-size: 0.95rem;
   color: #555;
   text-align: right;
 }
 
 .quote-author strong {
   color: var(--wood-dark);
 }
 
 .quote-position {
   font-size: 0.85rem;
   color: #777;
   font-style: italic;
 }
 
 .hero-description {
   margin-top: 30px;
 }
 
 .hero-description p {
-  font-size: 1.05rem;
+  font-size: calc(1.05rem * var(--body-font-scale));
   color: #555;
-  line-height: 1.8;
+  line-height: calc(1.8 * var(--body-line-scale));
   margin-bottom: 20px;
   text-align: justify;
 }
 
 .hero-description p:last-child {
   margin-bottom: 0;
 }
 
 /* =====================
    AUTHOR
    ===================== */
 
 .badge {
   display: inline-flex;
   align-items: center;
   gap: 8px;
   background: #eee;
   padding: 5px 15px;
   border-radius: 20px;
   font-size: 0.9rem;
   margin-bottom: 25px;
 }
 
 .author-layout {
   display: grid;
@@ -291,54 +316,54 @@ body {
   object-fit: cover;
   object-position: center;
   transition: transform 0.3s ease;
 }
 
 .author-photo-wrapper:hover .author-photo {
   transform: scale(1.05);
 }
 
 .author-text-content {
   padding-top: 10px;
 }
 
 .author-text-content-full {
   max-width: 100%;
 }
 
 .author-name {
   font-size: 2rem;
   color: var(--wood-dark);
   margin-bottom: 25px;
   line-height: 1.2;
 }
 
 .author-text {
-  font-size: 1.1rem;
+  font-size: calc(1.1rem * var(--body-font-scale));
   color: #555;
   margin-bottom: 18px;
-  line-height: 1.75;
+  line-height: calc(1.75 * var(--body-line-scale));
   text-align: justify;
 }
 
 .author-text:last-child {
   margin-bottom: 0;
 }
 
 /* =====================
    BOOKSHELF
    ===================== */
 
 .shelf-group {
   margin-bottom: 80px;
 }
 
 .bookshelf {
   position: relative;
   display: flex;
   flex-wrap: wrap;
   justify-content: center;
   gap: 25px;
   padding: 20px 20px 40px;
   background: var(--wood-color);
   border-radius: 4px;
   border-bottom: 15px solid var(--wood-dark);
@@ -523,52 +548,52 @@ body {
   transform: translateY(-2px);
 }
 
 .article-header-v4 {
   display: flex;
   flex-direction: column;
   gap: 12px;
   margin-bottom: 16px;
 }
 
 .article-divider-v4 {
   height: 2px;
   background: linear-gradient(90deg, var(--wood-color), transparent);
 }
 
 .article-title-v4 {
   color: var(--wood-dark);
   font-size: 1.3rem;
   font-weight: 600;
   margin: 0;
   line-height: 1.4;
 }
 
 .article-preview-v4 {
   color: #666;
-  font-size: 1rem;
-  line-height: 1.7;
+  font-size: calc(1rem * var(--body-font-scale));
+  line-height: calc(1.7 * var(--body-line-scale));
   flex-grow: 1;
 }
 
 /* =====================
    REVIEWS SECTION - GRID LAYOUT
    ===================== */
 
 .reviews-grid {
   display: grid;
   grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
   gap: 30px;
   margin-top: 30px;
 }
 
 .review-card {
   background: #fff;
   border: 2px solid #e0e0e0;
   border-radius: 8px;
   overflow: hidden;
   transition: all 0.3s ease;
   display: flex;
   flex-direction: column;
 }
 
 .review-card:hover {
@@ -597,69 +622,69 @@ body {
 .review-card:hover .review-book-cover img {
   transform: scale(1.05);
 }
 
 .review-card-content {
   padding: 25px;
   flex-grow: 1;
   display: flex;
   flex-direction: column;
 }
 
 .review-book-title {
   font-size: 1.2rem;
   font-weight: 600;
   color: var(--wood-dark);
   margin-bottom: 20px;
   line-height: 1.3;
 }
 
 .review-item {
   margin-bottom: 15px;
 }
 
 .review-text {
   color: #555;
-  line-height: 1.7;
+  line-height: calc(1.7 * var(--body-line-scale));
   margin-bottom: 10px;
-  font-size: 0.95rem;
+  font-size: calc(0.95rem * var(--body-font-scale));
 }
 
 .review-quote {
   background: rgba(139, 90, 43, 0.05);
   border-left: 4px solid var(--accent);
   padding: 15px;
   margin: 15px 0;
   border-radius: 4px;
   font-style: italic;
 }
 
 .review-quote p {
-  font-size: 1rem;
+  font-size: calc(1rem * var(--body-font-scale));
   color: var(--text-main);
   margin-bottom: 8px;
-  line-height: 1.6;
+  line-height: calc(1.6 * var(--body-line-scale));
 }
 
 .review-author {
   text-align: right;
   font-style: normal;
   font-size: 0.9rem;
   color: #777;
   margin-top: 8px;
 }
 
 .btn-open-review {
   display: inline-block;
   margin-top: auto;
   padding: 10px 20px;
   background: var(--wood-color);
   border: none;
   color: white;
   font-weight: 600;
   border-radius: 6px;
   cursor: pointer;
   transition: all 0.3s ease;
   font-family: 'Georgia', serif;
   font-size: 0.95rem;
   align-self: flex-start;
 }
@@ -742,52 +767,52 @@ body {
   color: #e74c3c;
 }
 
 .iframe-container {
   flex-grow: 1;
   background-color: #525659;
   overflow: hidden;
 }
 
 .iframe-container iframe {
   width: 100%;
   height: 100%;
   border: none;
 }
 
 /* Article Modal Specific */
 .article-content-scroll {
   flex-grow: 1;
   overflow-y: auto;
   padding: 50px 60px;
   background: #ffffff;
 }
 
 .article-full-text {
   font-family: 'Georgia', serif;
-  font-size: 1.15rem;
-  line-height: 1.9;
+  font-size: calc(1.15rem * var(--body-font-scale));
+  line-height: calc(1.9 * var(--body-line-scale));
   color: var(--text-main);
 }
 
 .article-full-text p {
   margin-bottom: 1.5em;
   text-align: justify;
   text-indent: 2em;
 }
 
 .article-full-text p:first-of-type {
   text-indent: 0;
 }
 
 .article-full-text blockquote {
   margin: 2em 0;
   padding: 1.5em 2em;
   background: rgba(139, 90, 43, 0.05);
   border-left: 4px solid var(--accent);
   font-style: italic;
   color: #555;
 }
 
 /* =====================
    REVIEW MODAL - "САЙТ В САЙТІ"
    ===================== */
@@ -831,68 +856,68 @@ body {
   color: var(--wood-dark);
   margin-bottom: 20px;
   margin-top: 30px;
 }
 
 .review-page-content h1 {
   font-size: 2.2rem;
   text-align: center;
   border-bottom: 3px solid var(--wood-color);
   padding-bottom: 15px;
   margin-bottom: 40px;
 }
 
 .review-page-content h2 {
   font-size: 1.8rem;
   border-left: 5px solid var(--accent);
   padding-left: 15px;
 }
 
 .review-page-content h3 {
   font-size: 1.4rem;
   color: #555;
 }
 
 .review-page-content p {
-  font-size: 1.1rem;
-  line-height: 1.8;
+  font-size: calc(1.1rem * var(--body-font-scale));
+  line-height: calc(1.8 * var(--body-line-scale));
   margin-bottom: 20px;
   text-align: justify;
 }
 
 .review-page-content blockquote {
   background: #fff;
   border-left: 5px solid var(--accent);
   padding: 25px 30px;
   margin: 30px 0;
   border-radius: 4px;
   font-style: italic;
   box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
 }
 
 .review-page-content blockquote p {
-  font-size: 1.15rem;
+  font-size: calc(1.15rem * var(--body-font-scale));
   margin-bottom: 10px;
 }
 
 .review-page-content .quote-author {
   text-align: right;
   font-style: normal;
   font-size: 1rem;
   color: #777;
   margin-top: 15px;
 }
 
 .review-page-content .review-section {
   background: #fff;
   padding: 30px;
   margin: 30px 0;
   border-radius: 8px;
   box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
 }
 
 .review-page-content .review-divider {
   height: 2px;
   background: linear-gradient(90deg, transparent, var(--wood-color), transparent);
   margin: 40px 0;
 }
 
@@ -905,52 +930,52 @@ body {
 }
 
 /* =====================
    DISQUS COMMENTS
    ===================== */
 
 #disqus_thread {
   min-height: 400px;
   padding: 20px;
 }
 
 .comments-wrapper {
   min-height: 400px;
   background: #fff;
   border-radius: 8px;
   padding: 20px;
   box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
 }
 
 .feedback-header {
   margin-bottom: 30px;
 }
 
 .feedback-description {
   color: #666;
-  font-size: 1.05rem;
-  line-height: 1.6;
+  font-size: calc(1.05rem * var(--body-font-scale));
+  line-height: calc(1.6 * var(--body-line-scale));
   margin-top: 10px;
 }
 
 /* =====================
    EXPANDABLE QUOTE FUNCTIONALITY
    ===================== */
 
 .quote-wrapper {
   position: relative;
   max-height: 3.5em; 
   overflow: hidden;
   transition: max-height 0.5s ease;
 }
 
 .quote-wrapper.expanded {
   max-height: 2000px; 
 }
 
 .quote-gradient-overlay {
   position: absolute;
   bottom: 0;
   left: 0;
   right: 0;
   height: 40px;
   pointer-events: none;
@@ -1001,58 +1026,96 @@ body {
 /* =====================
    RESPONSIVE
    ===================== */
 
 @media (max-width: 968px) {
   .author-layout {
     grid-template-columns: 200px 1fr;
     gap: 30px;
   }
   .author-name { font-size: 1.7rem; }
   .divider-quote-text { font-size: 1.5rem; }
   .reviews-grid {
     grid-template-columns: 1fr;
   }
 }
 
 @media (max-width: 768px) {
   .hero-top-row {
     grid-template-columns: 1fr;
   }
 
   .hero-photo-wrapper {
     max-width: 220px;
   }
 
-  .nav-links { display: none; }
+  .menu-toggle {
+    display: inline-flex;
+    align-items: center;
+    justify-content: center;
+    margin-left: auto;
+  }
+
+  .nav-links {
+    display: none;
+    width: 100%;
+    order: 4;
+    flex-direction: column;
+    align-items: flex-start;
+    gap: 12px;
+    background: #fff;
+    border: 1px solid rgba(139, 90, 43, 0.2);
+    border-radius: 10px;
+    padding: 14px;
+    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.08);
+  }
+
+  .nav-links.open {
+    display: flex;
+  }
+
+  .nav-container {
+    flex-wrap: wrap;
+    align-items: center;
+  }
+
+  .logo {
+    font-size: 1.1rem;
+  }
+
+  .lang-switcher {
+    order: 3;
+    margin-left: 0;
+  }
+
   .lang-switcher button { padding: 5px 10px; font-size: 0.85rem; }
   .hero-card { padding: 30px 20px; }
   .author-layout { grid-template-columns: 1fr; gap: 25px; }
   .author-photo-wrapper { max-width: 280px; margin: 0 auto; }
   .author-name { font-size: 1.8rem; text-align: center; }
   .books-with-comments {
   display: grid;
   grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
   gap: 18px;
   align-items: start;
 }
 
 .books-with-comments .book-spine {
   margin: 0 auto;
 }
 
 .book-spine { width: 130px; height: 190px; }
   .articles-grid-v4 { grid-template-columns: 1fr; }
   .reviews-grid { grid-template-columns: 1fr; }
   .section-card { padding: 20px; }
   .article-content-scroll { padding: 30px 20px; }
   .review-page-content { padding: 30px 15px; }
   .modal-content { width: 100%; height: 100%; border-radius: 0; }
   .modal-header { flex-direction: column; gap: 15px; align-items: flex-start; }
   
   .quote-wrapper { max-height: 8em; }
   .quote-wrapper:not(.expanded) .quote-gradient-overlay { height: 5em; }
   
   .review-page-content h1 { font-size: 1.6rem; }
   .review-page-content h2 { font-size: 1.4rem; }
-  .review-page-content p { font-size: 1rem; }
-}
\ No newline at end of file
+  .review-page-content p { font-size: calc(1rem * var(--body-font-scale)); }
+}
